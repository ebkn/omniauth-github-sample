<%= form_for @user do |f| %>
  <h2>Githubアカウント名</h2>
  <%= current_user.github_name %>
  <h2>ユーザー名</h2>
  <%= current_user.user_name %>
  <h2>名前</h2>
  <%= f.text_field :name %>
  <%= f.submit '登録' %>
<% end %>
